spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3308}/${DATABASE_NAME:jobportal}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}

cache.jobs.ttl-minutes=15
cache.companies.ttl-minutes=15

# CORS configuration
app.cors.allowed-origins=http://localhost:5173,http://qa.jobportal.portal.com